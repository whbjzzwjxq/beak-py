"""Small runnable demos / harnesses that exercise beak-core primitives.

These modules are *not* part of the core library API surface; keep imports local
to avoid pulling optional deps (e.g. specific zkVM adapters) into beak-core.
"""

