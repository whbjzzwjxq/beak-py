[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "beak-core"
version = "0.1.0"
description = "Core logic for beak fuzzer"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
include = ["beak_core*"]

